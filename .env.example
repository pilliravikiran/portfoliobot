# =====================================================
# DO NOT put your real key in THIS file.
# This file is a TEMPLATE that gets committed to GitHub.
#
# Instead:
#   1. Make a COPY of this file named ".env" (no .example)
#   2. Put your REAL key in that .env file
#   3. .env is in .gitignore — never gets committed
#
# Get a key at: https://platform.openai.com/api-keys
# =====================================================

OPENAI_API_KEY=sk-your-key-here

# Optional — only change if you know why.
# CHAT_MODEL=gpt-4o-mini
# EMBEDDING_MODEL=text-embedding-3-small
